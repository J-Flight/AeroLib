function Quiz_Detect(): boolean;
begin
    if (not tabExists(TAB_MAGIC)) then
      if (GetMusic = 'Tbe Quizmaster') then
        result:= ((countMMDots('npc') = 1) and (percentblackmm>80));
end;

function Quiz_Solve(): boolean;
begin
  Result := false;
end;
